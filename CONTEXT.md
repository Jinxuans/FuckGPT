# Domain context

## 邮箱分配生命周期

邮箱从 provider 池中被领取开始，到注册成功、注册失败、任务取消、隔离、释放、归档或自然过期结束的完整状态过程。它是邮箱可再次分配与否的唯一业务语义。

## 邮箱分配

一次注册尝试对一个具体邮箱地址及其 provider 凭据的占用记录。邮箱分配先于 GPT 账户产生，因此不能依赖 GPT 账户主键作为身份。

邮箱分配保留历史状态：

- `active`：注册尝试正在使用。
- `succeeded`：注册成功并产生 GPT 账户关系。
- `failed`：注册失败，邮箱资源已经回池。
- `cancelled`：任务取消，邮箱资源已经回池。
- `interrupted`：进程异常退出，恢复时邮箱资源立即回池。

邮箱分配历史与邮箱资源当前状态分开保存。失败历史不会阻止邮箱资源再次分配。

## 邮箱资源状态

- `available`：可以被新的注册尝试领取。
- `allocated`：当前存在一个 active 邮箱分配。
- `bound`：已经关联成功注册的 GPT 账户，不能再次分配。
- `expired`：provider 已过期或明确不可使用。
- `archived`：从日常资源视图隐藏，但保留历史记录。

## 注册尝试

一个任务 worker 针对一个邮箱执行一次 GPT 注册的过程。注册尝试拥有稳定的尝试标识，并负责把邮箱分配推进到成功、失败或取消等终态。

## 邮箱关系

邮箱分配与最终 GPT 账户之间的关联。注册成功后，邮箱关系引用 GPT 账户；注册失败时，邮箱关系仍只引用注册尝试，不产生虚假的 GPT 账户关系。

成功关系遵循以下约束：

- 一个邮箱地址最多关联一个成功注册的 GPT 账户。
- 一个 GPT 账户只有一个主要验证邮箱。
- 别名地址是独立邮箱资源，可以分别关联不同 GPT 账户。
- 父邮箱账号只保存 provider 登录凭据，不直接关联 GPT 账户。
- 成功关系使用数据库外键，不依赖邮箱字符串或 JSON link 推断。
- 删除 GPT 账户时保留历史关系，并把对应邮箱资源归档；不能自动回池。

## 资源策略

不同 provider 的邮箱分配策略：

- 只有 GPT 账户成功写入本地账户库才算注册成功。
- 注册失败、任务取消或异常退出：立即释放邮箱分配并回池，不设置隔离期。
- 购买邮箱与临时邮箱只要仍可使用，也遵循失败即回池；provider 已过期或明确不可复用时标记过期。
- 成功注册：进入已关联状态，不能直接释放。
- 重新分配邮箱时必须以当前邮件基线过滤旧邮件，但系统不承诺清除 provider 或目标平台的远端历史状态。

## 约束

- SQLite 是邮箱分配生命周期的唯一事实来源。
- provider adapter 只负责 provider 特有的领取、收件和远端资源操作。
- JSON 邮箱资源文件只承担迁移与兼容读取，不能继续独立决定邮箱是否可用。
- 旧 JSON 数据只迁移一次：成功 GPT 账户关系导入为 `bound/succeeded`，没有成功关系的旧占用导入为 `interrupted` 并立即回池。
- provider 登录账号与邮箱地址分表保存；多个 alias 资源共享一份父邮箱登录凭据，GPT 账户图不再复制这份凭据。
- 释放与删除分离：释放改变可分配状态，删除只做归档。
